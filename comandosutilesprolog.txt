En prolog podemos definir 2 tipos de variables:
-reglas
-facts (verdades como male(bob))

Cuando usamos incognitas en los atomos(X) ciclamos usando (;)

cargar bases de datos-> [NOMBREARCHIVO].
HALT. -> quit swipl
write() -> funcion para printear
listing()-> funcion para listar todos los atomos que cumplen una condicion

operador pito (:-) significa if, true si se cumple una condicion(->)
operador fail (da false)
operador ! (para el backtracking)

hay comandos de archivos
-open
-close
-write
-delete(L1,X,L3)
-member()
-between()


